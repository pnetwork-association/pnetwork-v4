{
  "name": "@pnetwork/eos-xerc20",
  "version": "1.0.0",
  "packageManager": "yarn@4.3.1",
  "license": "BSL",
  "files": [
    "/contracts"
  ],
  "scripts": {
    "build": "make all",
    "clean": "make clean",
    "test": "yarn build && mocha",
    "nodeos": "nodeos --config-dir . -c config.ini --delete-all-blocks > /dev/null >> nodeos.log 2>&1 &"
  },
  "devDependencies": {
    "@eosnetwork/vert": "^1.0.0",
    "@types/ramda": "^0",
    "@wharfkit/antelope": "^1.0.12",
    "chai": "^4.3.10",
    "mocha": "^10.2.0",
    "ramda": "^0.30.1"
  }
}
